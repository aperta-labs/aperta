{
  "name": "@aperta/store",
  "version": "1.0.0",
  "description": "A delightful (and fast) way to cache or rate limit",
  "main": "./build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun build ./src/index.ts --outdir ./build --minify --target node && bun run build:declaration",
    "build:declaration": "tsc --emitDeclarationOnly --project tsconfig.types.json",
    "release": "bun run build && changeset publish"
  },
  "author": {
    "name": "Liam Grossman"
  },
  "license": "ISC",
  "devDependencies": {
    "redis": "^5.1.1",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "redis": "^5.1.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
